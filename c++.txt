Map: 

map < int, int > my_map1;
my_map.insert( make_pair(item, price));

map < string, pair<int, int> > my_map2;
my_map2.insert( make_pair(key, make_pair(item, price)));


map < string, pair < int, int> >::iterator it = my_map2.find("key");
if (it != my_map2.end()){}

for (it = my_map2.begin(); it != my_map2.end(); it++)
{
  if (it->second.first == 1)
  {...}
}

bool getPair(pair <int, double> &p){
  p.first = A;
  p.second = B;
}


    

Math: 
#include <math.h>
floor()



